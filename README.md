# PHP Rename

`php-noobs/php-rename` is a PHP refactoring library focused on safe class and member renaming.

The package consumes `php-noobs/member-graph` for semantic dependency facts and keeps source mutation in this package boundary.

## Documentation

Project documentation starts in [doc/README.md](doc/README.md).
